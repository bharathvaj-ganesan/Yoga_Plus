<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.proudtobeanindian.yogaplus">

    <uses-sdk android:maxSdkVersion="24" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Home_Activity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Thirty_days"
            android:label="30 Days of Yoga"
            android:parentActivityName=".Home_Activity"/>
        <activity android:name=".UpaTools"
            android:label="Tools for life"
            android:parentActivityName=".Home_Activity"/>
        <activity android:name=".YoutubeActivity"
            />
    </application>

</manifest>